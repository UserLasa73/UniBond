module.exports = {
  presets: [
    "module:metro-react-native-babel-preset", // Preset for React Native projects
  ],
  plugins: [
    "react-native-reanimated/plugin", // Keep this for React Native Reanimated
  ],
};
